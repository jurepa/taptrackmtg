<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Empires" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>
        <item name="colorOnPrimary">@color/onPrimary</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondaryVariant</item>
        <item name="colorOnSecondary">@color/onSecondary</item>

        <!-- Background and surface -->
        <item name="android:colorBackground">@color/onBackground</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorSurface">@color/textSecondary</item>
        <item name="colorOnSurface">@color/onSurface</item>

        <!-- Status bar and navigation bar -->
        <item name="android:statusBarColor" >@color/primaryVariant</item>
        <item name="android:navigationBarColor">@color/background</item>

        <!-- Typography -->
        <item name="android:textColor">@color/background</item>
        <item name="android:textColorPrimary">@color/background</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>
    </style>
</resources>